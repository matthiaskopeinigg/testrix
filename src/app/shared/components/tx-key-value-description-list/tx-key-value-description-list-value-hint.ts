/** Shown on the Value field help icon when {@link TxKeyValueDescriptionListComponent} uses variable values. */
export const TX_KEY_VALUE_DESCRIPTION_LIST_VALUE_HINT =
  'In values, type $ for dynamic placeholders such as $uuid, $timestamp, and $randomInt(6).\n' +
  'They resolve when a request is sent, not when you save.';
