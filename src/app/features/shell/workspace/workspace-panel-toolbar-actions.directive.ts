import { Directive } from '@angular/core';

/** Marks projected toolbar actions for {@link WorkspaceSidebarPanelShellComponent}. */
@Directive({
  selector: '[workspacePanelToolbarActions]',
  standalone: true,
})
export class WorkspacePanelToolbarActionsDirective {}
