/**
 * Included by electron-builder into the generated NSIS script.
 * Customize welcome/progress copy here; uninstall-specific flows live alongside in `uninstaller.nsh`.
 */
!macro customInstall
  DetailPrint "Testrix setup uses the familiar Windows wizard. Configuration files stay outside the install folder."

!macroend
